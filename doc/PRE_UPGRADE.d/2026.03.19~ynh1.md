This is a huuuge package update!
- The compilation and download issues have been addressed. It should work out of the box now.
- Now we install Overleaf from tarballs built unofficially here: https://github.com/Thovi98/overleaf-builds/releases. This decrease the testing of the app by 3x on the CI, and maybe more on your server (it uses less resources anyway).
- A long time request was to install the latest TexLive distribution instead of relying on the one from the repo (from 2022…). It’s now done. You can visit the admin documentation in the `Webadmin > apps > Overleaf` for more information of how to proceed.